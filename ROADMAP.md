# ROADMAP

## Done
- [x] Initial exploration & project discovery.
- [x] Extract `handleGameOver` logic into `useGameLogicHelpers.ts`.
- [x] Split `hud.css` into modular style shards (`hud-core.css`, `hud-toasts.css`, etc.) to enforce max size limits.
- [x] Improve responsiveness with modern CSS (`100dvh`, `clamp`).
- [x] Tweak Framer Motion animation properties in `NewRoundBanner`, `FeedbackOverlay`, and `UrgentIndicator` for visceral fun.

## To Do
- [ ] Implement Release-Bot CI/CD Pipeline.
